import { Component, Input } from '@angular/core';

import { Member } from './model'

@Component({
  moduleId: module.id,
  selector: 'my-app',
  templateUrl: 'app.component.html'
})
export class AppComponent  { 
  title = 'Football Pool Stats'; 
}
